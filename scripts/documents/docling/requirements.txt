# Docling GCS Parser — Python dependencies
# VM image: common-cu129-ubuntu-2204-nvidia-580 (CUDA 12.9, driver 580)

# PyTorch — install separately with the cu124 index URL:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# (cu124 wheels are forward-compatible with CUDA 12.9+)

# ── Google Cloud ───────────────────────────────────────────────────────────────
google-cloud-storage>=2.16.0

# ── Docling core ───────────────────────────────────────────────────────────────
docling>=2.5.0

# ── RAR support ────────────────────────────────────────────────────────────────
rarfile>=4.1
# Also requires system package: sudo apt-get install unrar-free