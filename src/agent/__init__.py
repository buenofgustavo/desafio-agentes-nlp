"""Camada de Agente LangGraph.

Expõe o grafo do agente compilado e a função de construção.

Uso::

    from src.agent.graph import agent_graph

    result = agent_graph.invoke({"original_query": "Qual é o prazo mínimo?"})
"""
